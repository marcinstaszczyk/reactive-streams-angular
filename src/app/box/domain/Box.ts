export class Box {

    // static boxes = new Map<string, Box>();
    //
    // static create(boxId: string): Box {
    //     let box: Box | undefined = this.boxes.get(boxId);
    //     if (box) {
    //         return box;
    //     } else {
    //         box = new Box(boxId);
    //         this.boxes.set(boxId, box);
    //         return box;
    //     }
    // }
    //
    // private constructor(
    //     readonly boxId: string
    // ) {}
}
